[options]
packages = find:
package_dir = =.

[options.packages.find]
where = .
include =
    thinkylm*
    data_pipeline*
    training*
    evaluation*
    inference*
    api*
    demo*
    scripts*
    tokenizer*
